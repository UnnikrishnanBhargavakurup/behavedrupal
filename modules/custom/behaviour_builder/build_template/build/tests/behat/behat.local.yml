# Local behat settings.
default:
  extensions:
    Behat\MinkExtension:
      browser_name: chrome
      base_url: 'LOCALHOST'
    Drupal\DrupalExtension:
      text:
        #username_field: "E-mail"
